[package]
name = "moongate-perf"
version = "0.1.0"
edition = "2021"

[dependencies]
sp1-prover = { workspace = true }
sp1-core = { workspace = true }

moongate-prover = { path = "../prover" }
moongate-core = { path = "../core" }

tracing = "0.1.40"
csv = "1.3.0"
